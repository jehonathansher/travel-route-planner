# macOS
.DS_Store

# Editor / OS junk
*.swp
Thumbs.db

# Node (only if you ever add tooling — not needed to run the app)
node_modules/

# NOTE: config.js IS committed on purpose. For a static site on GitHub Pages
# the config must be served to the browser, and the values are safe to expose
# when the Maps key is referrer-restricted (see README "Security & cost").

# Local editor/preview tooling (machine-specific)
.claude/
